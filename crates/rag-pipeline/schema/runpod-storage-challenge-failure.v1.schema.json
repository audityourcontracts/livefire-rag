{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://livefire.dev/rag/runpod-storage-challenge-failure.v1.schema.json",
  "title": "RunPod mounted-storage challenge failure",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "executor_image", "challenge", "status", "publication"],
  "properties": {
    "schema_version": {"const": "livefire.rag.runpod-storage-challenge-failure/1"},
    "executor_image": {"type": "string", "pattern": "^[^\\s]+@sha256:[0-9a-f]{64}$"},
    "challenge": {"$ref": "#/$defs/object"},
    "status": {"const": "failed"},
    "publication": {"const": "hard_link_no_overwrite"}
  },
  "$defs": {
    "digest": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "path": {"type": "string", "minLength": 1, "pattern": "^(?!/)(?!.*(?:^|/)(?:\\.|\\.\\.)(?:/|$))(?!.*//)(?!.*[\\\\:])[^/]+(?:/[^/]+)*$"},
    "object": {
      "type": "object",
      "additionalProperties": false,
      "required": ["key", "bytes", "sha256"],
      "properties": {
        "key": {"$ref": "#/$defs/path"},
        "bytes": {"type": "integer", "minimum": 1, "maximum": 1048576},
        "sha256": {"$ref": "#/$defs/digest"}
      }
    }
  }
}
