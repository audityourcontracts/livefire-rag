# rag-testkit

Format-faithful fixture helpers for the Rust experimental path. The
`make-smoke-snapshot` binary creates a tiny typed OCSF snapshot and frozen
synthetic query/qrel rows for interface testing with the real local model.
